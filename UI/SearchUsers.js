import React from 'react';
import SearchPage from '@modules/search/SearchPage';

const SearchUsers = () => {
  return (
    <div>
      <SearchPage type={users} />
    </div>
  );
};

export default SearchUsers;
